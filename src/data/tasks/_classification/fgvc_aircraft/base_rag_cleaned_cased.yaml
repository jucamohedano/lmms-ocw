task: "fgvc_aircraft_rag_cleaned_cased"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "What type of object is in this photo?"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False
  rag:
    enabled: True
    database_path: "cc12m-cleaned/openai_ViT-B-32/index"
    model_name: "openai/clip-vit-base-patch32"
    prompt: "Use these words that have been retrieved because they best describe the image as additional context: {}"

    caption_key: "caption_full"
    format: "cased"
include: assets/_default_template_yaml
