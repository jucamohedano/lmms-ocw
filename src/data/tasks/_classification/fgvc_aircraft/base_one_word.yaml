task: "fgvc_aircraft_one_word"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "Classify this image in one word."
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False

include: assets/_default_template_yaml
