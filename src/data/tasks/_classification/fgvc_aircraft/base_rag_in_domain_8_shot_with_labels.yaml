task: "fgvc_aircraft_rag_in_domain_8_shot_with_labels"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "Answer:"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False
  resize_input_image: 224
  rag:
    enabled: True
    database_path: "cc12m/openai_ViT-B-32/index"
    model_name: "openai/clip-vit-base-patch32"
    system_prompt: "You are a precise image classifier. Classify the given image into **exactly one** of the categories below. Output **only the chosen category name**, nothing else."
    prompt: "Here are some examples of input-output pairs:\n<images>Answer: {}"
    search_modality: "in-domain"
    grab_data_type: "image"
    format: "interleaved"
    limit: 8
    shuffle: True
    resize_images: 224
    include_target_classes: "pre-query"
    target_classes_format: "bullet-list"
    target_classes_prompt: "What object is in the photo? Choose exactly one:\n{}"

include: assets/_default_template_yaml
