task: "fgvc_aircraft_very_specific"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "Provide the label for the object in the image. Classify the object in the most fine-grained way possible. For instance, if you see a 'labrador', respond with 'labrador' and not 'dog'.\nFine grained label:"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False

include: assets/_default_template_yaml
