task: "food101_open_random_in_domain_16_shot_final"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "What object is in the photo?"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False
  rag:
    enabled: True
    database_path: "food101/train/openai-clip-vit-base-patch32"
    model_name: "openai/clip-vit-base-patch32"
    prompt: "Here are some examples of input-output pairs:\n<images>Answer: {}"

    database_format: "pt"
    search_modality: "random"
    grab_data_type: "image"
    format: "interleaved"
    num_samples: 16
    resize_images: 224
    num_shots_per_class: 16
  resize_input_image: 224
include: assets/_default_template_yaml
