task: "eurosat_taxonomy"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "What's the object in the image? Provide *only* a taxonomy from a general category to a very specific one, separating them by commas"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False

include: assets/_default_template_yaml
