task: "oxford_pets_rag_post_vitl_cased"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "What type of object is in this photo?"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False
  rag:
    enabled: True
    database_path: "cc12m/openai_ViT-L-14/index"
    model_name: "openai/clip-vit-large-patch14"
    prompt: "Use these words that have been retrieved because they best describe the image as additional context: {}"
    position: "post-sample"

    format: "cased"
include: assets/_default_template_yaml
